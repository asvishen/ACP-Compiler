function abc(int a)
<<
        int b=2;
        if(a==1 and b<=2)
        <<
                print [both conditions satisfy];
        >>
        else
        <<
                print [not satisfied];
        >>
	return 1;
>>
int call = abc(1);